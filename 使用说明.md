# 明日路演｜从烧录到双板联动

## 0. 先记住三件事

- T5 与 ESP32 是两块独立主板，分别烧录、分别供电。
- 当前路演中，T5 负责摄像头画面；ESP32 负责旋钮、敲击和四项控制事件。
- Mac 是中枢：接收两块板的数据、使用 Mac 麦克风，并在 Chrome 保存 WebM。

烧录前先读 [`hardware/docs/NICEMCU-T5-DEV_V1.2_主板事实表.md`](./hardware/docs/NICEMCU-T5-DEV_V1.2_主板事实表.md)。这块板还设计了麦克风、扬声器、屏幕、触摸、TF 卡、加速度计、温度传感器、RGB、LoRa、电池管理和 PWM 接口；当前固件只承诺 OV2640 出流，其他能力需要分别实测。

先不要接旋钮和敲击传感器。先依次验证 T5、ESP32 自检、ESP32 串口指令、网站，最后再接传感器。

## 1. 需要烧录的三个文件

### T5 摄像头固件

```text
firmware/t5-camera/bin/WhisperHands_T5_camera_QIO_1.0.0.bin
```

用你已经验证成功的 TuyaTool 手动烧录。请选择文件名中带 `QIO` 的这一份。不要选 TuyaOpen 原始输出中的 `UA` 或 `UG` 文件。

### ESP32 首次自检固件

```text
firmware/esp32-self-test/bin/WhisperHands_esp32-self-test_merged_16MB.bin
```

这是第一份要烧进 ESP32 的文件，用来确认 USB、烧录和串口正常。它是完整 merged 文件，烧录地址为 `0x0`。

### ESP32 控制联动固件

```text
firmware/esp32-control/bin/WhisperHands_esp32-control_merged_16MB.bin
```

自检通过后再烧这一份。它同样是完整 merged 文件，烧录地址为 `0x0`。

校验值见 [`firmware/CHECKSUMS.sha256`](./firmware/CHECKSUMS.sha256)。

## 2. 烧录并检查 T5

1. Mac 可以先连接有互联网的普通 Wi‑Fi；编译已经完成，烧录不依赖热点。
2. 用 USB 连接 T5。
3. 打开 TuyaTool，选择 T5 串口。
4. 选择 `WhisperHands_T5_camera_QIO_1.0.0.bin`。
5. 开始烧录，等待工具显示成功。
6. 烧录完成后打开 **`460800`** 波特率串口日志（T5AI 平台标准波特率）。
7. 重启 T5，等待它连接临时 2.4GHz 热点。
8. 从上电开始保存完整日志，重点保留 audio、display、touch、camera、SDIO 的注册结果。当前 BSP 会初始化多项板载外设，即使主演示只使用摄像头。
9. 在日志中找到类似下面的一行，记下其中的 IP：

```text
WiFi up. Open http://172.20.10.x/ in a browser
```

10. Mac 也连同一个 2.4GHz 热点。
11. 在 Chrome 分别打开 `http://T5的IP/stream` 和 `http://T5的IP/snapshot`。能持续看到摄像头画面并取得单帧图片，T5 单板验证完成。

如果没有画面，把完整串口日志保存到 `handoff/checkpoints/`，不要马上改网站。

## 3. 烧录并检查 ESP32 自检版

### 方法 A：VS Code + PlatformIO

1. 在 VS Code 中打开 `firmware/esp32-self-test` 文件夹。
2. 用 USB 连接 ESP32。
3. 点击 PlatformIO 的 Upload。
4. 打开 Serial Monitor，波特率设为 `115200`。

### 方法 B：使用已生成的 merged 文件

先在终端查看串口名：

```bash
ls /dev/cu.*
```

将下面的 `/dev/cu.usbmodemXXXX` 换成实际串口，再执行：

```bash
./.venv-platformio/bin/python "$HOME/.platformio/packages/tool-esptoolpy/esptool.py" \
  --chip esp32s3 --port /dev/cu.usbmodemXXXX --baud 460800 \
  write_flash 0x0 firmware/esp32-self-test/bin/WhisperHands_esp32-self-test_merged_16MB.bin
```

打开串口监视器：

```bash
./.venv-platformio/bin/pio device monitor --port /dev/cu.usbmodemXXXX --baud 115200
```

看到以下内容即通过：

```text
Whisper Hands ESP32-S3 self-test
我已开机
BOARD=ESP32-S3-N16R8
STATUS=READY
HEARTBEAT uptime_ms=...
```

按 `Control+C` 退出串口监视器。

## 4. 烧录并检查 ESP32 控制版

自检通过后，将上一步烧录命令里的文件换成：

```text
firmware/esp32-control/bin/WhisperHands_esp32-control_merged_16MB.bin
```

仍然从 `0x0` 烧录。重启后串口应出现：

```text
Whisper Hands ESP32-S3 control
我已开机
WIFI_CONFIG=READY
BRIDGE_CONFIG=READY
```

暂时不接传感器也能测试。在串口逐行发送：

```text
1
2
3
3
4
```

对应录像切换、截图、重点语音开始、重点语音结束、结束录像。

## 5. 启动 Mac 网站桥接

1. Mac、T5、ESP32 同时连接同一个 2.4GHz 热点。
2. 在终端进入本文件夹。
3. 把 T5 串口日志中的 IP 填进命令：

```bash
T5_BASE_URL=http://172.20.10.x npm start
```

4. 保持终端窗口开启。
5. 用 Chrome 打开：

```text
http://127.0.0.1:4173/
```

6. 允许 Chrome 使用 Mac 麦克风。

如果 Mac 重连热点后 IP 改变，在 ESP32 串口发送：

```text
HOST:Mac当前IP
```

ESP32 会保存新地址，无需重新编译。

## 6. 最短验收顺序

1. 网站实时区域能看到 T5 画面并连续运行 10 分钟。
2. 网页点击“录像”后，红色状态出现。
3. ESP32 串口发 `2`，网页生成截图。
4. ESP32 串口发 `3`，开始重点语音区间；再发一次 `3`，结束区间。
5. ESP32 串口发 `1`，能暂停或恢复。
6. ESP32 串口发 `4`，完成并保存记录。
7. 回放 WebM，确认同时有 T5 画面和 Mac 原声。
8. 刷新页面，确认个人实验库仍有本次记录。

## 7. 路演降级顺序

1. T5 直播 + Mac 收音 + ESP32 控制。
2. 备用样片 + Mac 收音 + ESP32 控制。
3. 备用样片 + Mac 收音 + 网页按钮。

备用样片已经接入本地桥接。T5 或 ESP32 临场断线时，不要修改代码，直接按下一层继续演示。

## 8. 每个重要节点必须存档

烧录成功、串口通过、T5 出流、ESP32 事件通过、完整录像通过、路演彩排通过后：

1. 更新 `handoff/00_先读_当前进度.md`；
2. 保存关键日志或截图；
3. 运行：

```bash
./scripts/save-checkpoint.sh 节点名称
```

示例：

```bash
./scripts/save-checkpoint.sh T5实机出流通过
```

脚本会在 `handoff/checkpoints/` 留下带时间的只读快照。下一位模型每次开始前必须先读最新快照。
