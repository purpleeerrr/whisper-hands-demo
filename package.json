{
  "name": "whisper-hands",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs tests-onboarding/*.test.mjs",
    "test:hardware": "node --test test/*.test.mjs",
    "test:onboarding": "node --test tests-onboarding/*.test.mjs"
  }
}