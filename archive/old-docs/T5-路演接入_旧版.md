# Whisper Hands 软硬件路演接入
目标：T5 OV2640 通过 Wi‑Fi MJPEG 给 Mac/网站画面，ESP32-S3 通过 HTTP 给 Mac 控制事件，Mac 麦克风合成 WebM。
当前阶段：实施后期，正在产出和核验两块板的可手动烧录 `.bin`。
已确认事实：T5 是 SPARKLEIOT_T5AI_DEV/OV2640；ESP32 是 ESP32-S3 N16R8；两板独立 USB 供电，不直连，都连同一 2.4GHz 网络。
已改动或已执行：网站已有 Node 桥接/SSE/T5 代理/Canvas+Mac 麦克风录像/截图/语音重点/转写/IndexedDB/样片降级；`npm test` 上次 24/24。
已改动或已执行：T5 `output_web` 已适配 OV2640，提供 `/stream` 和 `/snapshot`；无 Wi‑Fi 凭据版编译成功，但当前产物不可烧。
已改动或已执行：ESP32 `firmware/esp32-self-test` 已首次编译成功；`firmware/esp32-control` 已写好旋钮/敲击/串口/HTTP/持久化 `HOST:` 逻辑，待测试编译。
未解决问题：T5 构建缓存仍显示 Wi‑Fi 为空，需用 `tos.py config set` 写入已提供的临时热点后重编并核验；密码不写入交接文档。
下一步：修复 T5 配置→重编→检查产物；跑 ESP32 native test+两个固件编译；再跑网站测试和浏览器 QA。
验证命令或验收标准：`npm test`；`.venv-platformio/bin/pio test -e native`；`.venv-platformio/bin/pio run`；T5 构建缓存中 SSID 非空；禁止代用户烧录。
涉及文件：`server.mjs`、`app/`、`test/`、`firmware/`；T5 仓库的 `examples/peripherals/camera/output_web/`与 `boards/T5AI/SPARKLEIOT_T5AI_DEV/board_com_api.c`。
约束：保留原 HTML 设计与相对路径；网页按钮始终可降级；T5 与 ESP32 各烧各的 `.bin`；用户手动使用 TuyaTool/烧录工具。
