# Whisper Hands｜三目录合并记录

日期：2026-08-15

## 已合并来源

1. `Whisper-Hands-网站源码包/`
2. `whisper-hands-kb-and-analyse/`
3. `00_现行版本/`

## 合并结果

- `Whisper-Hands-网站源码包/` 成为唯一主项目和 Git 工作目录。
- 队友模块移动到 `backend/kb-and-analyse/`。
- 当前计划和待办移动到 `docs/project/`。
- 硬件说明和图片移动到 `hardware/docs/`、`hardware/assets/`。
- 4GB TuyaOpen 本地仓库移动到 `hardware/local-tuyaopen/`。
- 授权文件移动到 `hardware/private/`。
- 旧选题、调研、架构和网页进入项目根目录 `99_归档/`。

## 安全边界

`.gitignore` 排除以下内容：

- `firmware/`
- `.venv-platformio/`
- `hardware/local-tuyaopen/`
- `hardware/private/`

这些目录保留在本机供烧录和编译使用。公开 GitHub 不接收其中的固件、工具链或凭据。

## 当前未完成

- 队友后端仅完成目录迁移，运行链路尚未接入。
- T5 和 ESP32 固件尚未由用户烧录。
- Chrome 麦克风、WebM 和双板实机链路尚未验收。
- 本轮没有执行 Git 提交或推送。
